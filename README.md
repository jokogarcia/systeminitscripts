# System Init Scripts
Just a collection of scripts for my personal, to get up and running after installing a fresh linux install.
Tested only in Debian.

To run `sudo ./runme.sh [nvidia | amd | nographics]`

